const foo = 'bar';
console.log(foo);
foo;

// Running the command `node foo.js` returned "bar".
// Pasting into Node returned undefined for the first two lines of code and "bar" for the last.
// Pasting the code into Chrome returned "bar".
