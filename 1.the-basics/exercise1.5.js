// Explain why this code logs '510' instead of 15.
// console.log('5' + 10);

/* The code logs '510' due to implicit type coercion. When a string value is involved in an operation with a non-string value,
   JavaScript will attempt to coerce the non-string value to a String type. */
