// The first console.log() statement outputs 'hi'. The bar function mutates the original object, changing its a property to the 'hi' value.
// The second console.log() statement outputs 'hello'. The bar function can't mutate a primitive, and thus the value of qux is still 'hello'.
